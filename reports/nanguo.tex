%!TeX program = xelatex
\documentclass{SYSUReport}

\usepackage{float}
\usepackage{longtable}
\usepackage{array}
\usepackage{tabularx}
\usepackage{caption}
\usepackage{xurl}

\headl{多智能体系统设计与实现}
\headr{人工智能实验7——智能体实验}
\lessonTitle{人工智能实验7——智能体实验}
\reportTitle{南郭乐团：多智能体音乐创作系统}
\inst{计算机学院}
\major{保密管理}
\stuname{吕梓文}
\stuid{24353028}
\date{\today}

\setlength{\parindent}{2em}
\setlength{\parskip}{0.25em}
\setlength{\headheight}{15pt}
\renewcommand{\arraystretch}{1.28}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\begin{document}

\cover
\updateheader{南郭乐团：多智能体音乐创作系统}
\thispagestyle{empty}
\clearpage

\begin{abstract}
本实验设计并实现了一个基于 LangGraph 的本地多智能体音乐创作系统“南郭乐团”（Mr.NanGuos'）。名称灵感来自“滥竽充数”的典故。智能体创作音乐本质上只是对人类艺术的拙劣模仿，所以用智能体创作音乐当然是“滥竽充数”，这些智能体自然就是“南郭先生”了，它们共同组成了一个“南郭乐团”。

系统基于 LangGraph 框架编排多智能体，把音乐创作拆分为需求理解、工作流规划、作词、旋律、和声、节奏、即兴、演奏、编曲、音色设计、混音审查和生成提示词编译等职责，再依据作品类型搭建不同的工作流图。

对外的“对话南郭”负责多轮对话、作品协调与记忆读取；“指挥南郭”依据当前请求和有效偏好选择受约束的预设路径；各专业南郭通过 Pydantic 结构化状态逐步交接生成结果；最后由供应商适配层调用 Suno 兼容服务并完成本地下载、音频分析和波形渲染。

系统实现了三种互补的推理组织方式：以指挥南郭为核心的受约束计划--执行、参考音频分析中的工具调用式推理与行动，以及生成前由审听南郭（如果用户上传了演示音频）完成的反思审查。

记忆模块同时提供会话内短期记忆和跨会话长期记忆，依据“显式创作参数、当前请求、长期偏好、系统默认”的优先级开展创作。

工具调用模块则使用了自己编写的小工具，包括音频探测与变换、波形可视化、轻量演示音频合成、MusicXML 与 MIDI 导出和外部音乐生成等多类能力。

实验结果表明，系统能够在流行人声、古典器乐、电子器乐和影视配乐等场景下选择合适路径，完成人机对话、后台生成、进度反馈、作品归档与偏好管理。

\textbf{关键词：}多智能体系统；智能体编排；音乐生成；结构化状态；长期记忆；工具调用
\end{abstract}

\clearpage
\tableofcontents
\clearpage
\pagenumbering{arabic}
\setcounter{page}{1}

\section{实验题目与目标}

\subsection{实验题目}

使用开源智能体编排框架设计并实现一个多智能体系统。实验选取“本地音乐创作工作台”作为任务场景，系统名称为“南郭乐团”（Mr.NanGuos'）：多个具有明确音乐职责的智能体组成南郭乐团，由对话南郭与指挥南郭理解需求、安排分工，最终形成可播放、可下载、可恢复的音乐作品及其中间产物。

\subsection{实验目标}

本实验研究如何把开放、含糊且跨多个专业环节的音乐需求转化为可检查、可组合、可执行的协作过程。具体目标如下：

\begin{enumerate}
    \item 设计不少于四个、职责边界清楚的智能体，使其提示词、输入字段和输出字段与任务相匹配；
    \item 以图工作流组织协作，根据流行人声、器乐、电子、影视配乐等任务动态选择节点，允许纯器乐路径跳过作词南郭；
    \item 实现至少一种智能体推理或规划机制，并使规划结果受到系统白名单约束；
    \item 同时提供会话内短期记忆和跨会话长期记忆，保证当前明确需求优先于历史偏好；
    \item 接入至少两类工具能力，把语言模型不适合稳定完成的音频探测、文件变换、乐谱导出和服务调用交给确定性程序；
    \item 完成可本地运行的前后端系统，形成从创作输入到作品管理的闭环，并对实现效果进行实验分析。
\end{enumerate}

\section{需求分析与总体设计}

\subsection{任务特点}

音乐创作天然具有多阶段、强依赖和多解性。歌词必须服从主题与语言；旋律需要兼顾歌词重音、和声与节奏；编曲依赖此前形成的旋律、和声和动态计划；供应商提示词（即项目调用的 Suno API）又不能混入内部 JSON、文件路径或过多解释。因此若让一个模型一次完成所有工作，通常会出现职责混杂、前后矛盾、器乐作品误生成人声、结果难以校验等问题。

系统据此采用“领域决策由智能体完成，机械操作由工具完成，外部服务由供应商提供”的原则。智能体只负责需要语义判断和音乐规划的内容；FFmpeg、ffprobe、music21 等承担音频与乐谱处理；Suno 请求集中在供应商适配层；FastAPI 与本地文件存储负责运行生命周期。该划分使每个环节都有清楚的输入、输出与失败位置。

\subsection{项目结构}

项目按智能体职责、应用编排、领域模型和外部能力分层组织。下面给出精简后的整体目录树；其中省略了 \verb|.git|、\verb|.venv| 等隐藏目录，以及 \verb|tests|、\verb|list|、\verb|node_modules|、缓存和构建产物等不影响主体结构的内容。

\begin{minted}[linenos=false,fontsize=\small]{text}
theNanGuos/
|-- agents/          # 各专业南郭及通用智能体封装
|-- app/             # FastAPI、LangGraph、记忆与本地存储
|-- frontend/
|   |-- public/      # 前端静态资源
|   `-- src/         # React、TypeScript 与工作流画布
|-- lib/             # 日志、提示词、技能和乐谱等公共能力
|-- models/          # Pydantic 领域模型与共享状态
|-- prompts/         # 各南郭的系统提示词
|-- providers/       # Suno 兼容音乐生成服务适配
|-- skills/          # 按角色加载的技能说明
|-- tools/           # 音频分析、波形、Demo 与乐谱工具
|-- works/           # 生成的音乐、封面和可视化产物
|-- main.py          # 命令行工作流入口
|-- pyproject.toml   # Python 项目与依赖配置
|-- start.sh         # 后端和前端的一键启动脚本
`-- uv.lock          # uv 锁定的依赖版本
\end{minted}

其中 \verb|agents/|、\verb|prompts/| 与 \verb|skills/| 共同定义智能体能力，\verb|app/| 负责把这些能力编排为可执行工作流；\verb|models/| 提供结构化交接协议，\verb|tools/| 和 \verb|providers/| 则分别承接确定性本地工具与外部音乐生成服务。运行时数据集中写入 \verb|data/|、\verb|works/| 和 \verb|_logs/|，与源代码保持隔离。

\subsection{四层架构}

系统从上到下分为四层：

\begin{enumerate}
    \item \textbf{交互层}：React、TypeScript 与 React Flow 构成对话页、创作台、工作流画布、作品集和记忆库；
    \item \textbf{应用层}：FastAPI 处理会话、项目、上传、运行状态与作品接口，本地后端负责长时间生成任务；
    \item \textbf{智能体编排层}：LangGraph 管理条件分支，Pydantic 与 \verb|TypedDict| 管理共享状态，各南郭只更新自己负责的字段；
    \item \textbf{能力层}：本地文件系统保存数据，音频与乐谱工具产生中间产物，独立供应商适配器对接 Suno 兼容服务。
\end{enumerate}

其核心数据流可概括为：

\begin{center}
用户请求/参考音频 $\rightarrow$ 会话与偏好解析 $\rightarrow$ 指挥南郭规划
$\rightarrow$ 专业南郭协作 $\rightarrow$ 提示词南郭
$\rightarrow$ 演示音频与音乐生成 $\rightarrow$ 音频分析与作品归档。
\end{center}

图~\ref{fig:studio} 展示了系统的主要工作区。左侧输入作品名称、预设乐团、流派、语言、乐器、文字构想和参考音频；中央使用图展示智能体编排；右侧报告运行阶段；下方同时提供中间演示音频、最终音频、波形和提示词信息。它把内部图执行转换为用户能够理解和操作的创作流程。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/创作台.png}
    \caption{音乐创作工作台总览}
    \label{fig:studio}
\end{figure}

从图~\ref{fig:studio} 可进一步观察到，电子器乐任务的画布没有作词南郭，执行状态中的建档、参考、生成和完成四个阶段均已通过；同一页面还展示了 12 秒演示音频、两条完整音乐及其波形。该截图证明了条件路由、阶段反馈和产物可视化已经在实际任务中贯通。

\subsection{技术选型}

\begin{table}[H]
    \centering
    \caption{主要技术及其职责}
    \label{tab:stack}
    \begin{tabularx}{\textwidth}{L{3.0cm}L{3.3cm}Y}
        \toprule
        技术 & 所在层次 & 采用原因与职责 \\
        \midrule
        Python 3.12、uv & 基础环境 & 统一依赖解析、虚拟环境和运行命令，降低本地复现实验的成本。 \\
        LangGraph & 智能体编排 & 以 \verb|StateGraph| 表达节点、条件边和终止条件，适合可组合的多分支流程。 \\
        Pydantic & 数据边界 & 为创意简报、歌词、旋律、乐谱、记忆和运行结果提供校验，避免依赖自由文本解析关键字段。 \\
        FastAPI & 应用接口 & 提供类型明确的本地表述性状态转移接口、上传和静态作品访问。 \\
        React 与 TypeScript & 前端 & 构建对话、创作、记忆和作品管理界面；React Flow 显示执行路径。 \\
        FFmpeg 与 ffprobe & 音频工具 & 读取音频元数据，生成预览、波形和轻量演示音频。 \\
        music21 & 乐谱工具 & 根据受约束的乐谱规格导出 MusicXML 与 MIDI。 \\
        Suno 供应商适配器 & 外部生成 & 把供应商鉴权、参数限制、轮询和下载与各南郭的职责隔离。 \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{多智能体系统设计}

\subsection{角色划分与职责边界}

系统实际包含对话协调、参考分析、工作流规划和多个音乐专业角色，数量明显多于最低协作规模。每个角色具有独立系统提示词、限定输入字段和 Pydantic 输出模型。表~\ref{tab:agents} 给出各角色在完整链路中的定位。

\begin{longtable}{L{2.7cm}L{4.0cm}L{7.0cm}}
    \caption{智能体角色、输入和输出}\label{tab:agents}\\
    \toprule
    角色 & 主要读取内容 & 主要职责与结构化输出 \\
    \midrule
    \endfirsthead
    \toprule
    角色 & 主要读取内容 & 主要职责与结构化输出 \\
    \midrule
    \endhead
    对话南郭 & 当前消息、近期消息、会话摘要、记忆上下文、活动项目 & 进行自然对话，决定聊天、建项、生成、修改、查看作品或追问；提取明确偏好。 \\
    参考南郭 & 上传资产索引 & 通过白名单工具探测音频、生成短预览或波形，输出音频参考列表。 \\
    指挥南郭 & 用户请求、预设、记忆上下文、有效偏好 & 选择八种工作流之一，形成创意简报和各角色指令。 \\
    作词南郭 & 创意简报、用户请求、指挥指令 & 生成分段歌词、主题、副歌记忆点、语言和演唱提示；只出现在人声路径。 \\
    旋律南郭 & 简报、歌词、已有和声/节奏 & 规划速度、主题旋律、副歌记忆点和情绪弧；必要时生成受约束乐谱规格。 \\
    和声南郭 & 简报、歌词、旋律 & 规划调性中心、和弦进行、和声节奏和张力策略。 \\
    节奏南郭 & 简报、歌词、旋律与和声 & 规划律动、打击乐、节奏动机和能量曲线。 \\
    即兴南郭 & 爵士路径中的和声、节奏、旋律 & 规划独奏者、独奏曲式、即兴语汇、合奏互动与边界。 \\
    演奏南郭 & 旋律、节奏、即兴计划 & 规划演奏法、力度、乐手关系和人性化细节。 \\
    编曲南郭 & 前述音乐计划与音频参考 & 确定配器、段落发展、织体和制作方向。 \\
    音色南郭 & 简报与编曲计划 & 规划音色调色板、标志性声音、空间运动和质感。 \\
    审听南郭 & 全部主要音乐计划 & 在生成前审查平衡、低频、结构、风格漂移等风险，形成审听建议。 \\
    提示词南郭 & 全部结构化创作结果、有效偏好 & 把领域计划压缩为少于 500 字符的供应商可用提示词。 \\
    \bottomrule
\end{longtable}

这种角色边界体现了“最小必要读取”原则。例如作词南郭不读取供应商配置，提示词南郭不负责网络请求，Suno 也不参与音乐创意判断。作词南郭、旋律南郭和和声南郭之间交换歌词草稿、旋律计划、和声计划等结构化对象，后续节点直接读取相应字段。

\subsection{结构化状态交接}

LangGraph 的共享状态使用 \verb|TypedDict| 声明可选字段，每步结果再由 Pydantic 模型约束。例如 \verb|CreativeBrief| 把标题、语言、流派、情绪、主题、时长、速度、人声与结构统一成一个可校验对象；\verb|NoteEvent| 对音高、时值和力度设定范围；最终提示词也有长度上限。每个通用智能体封装只根据 \verb|input_fields| 构造输入，并只返回输出模型定义的字段。

\noindent\textbf{代码 1：通用智能体的结构化输入输出机制}（原样引自 \verb|agents/base.py|）
\inputminted[firstline=78,lastline=120]{python}{../agents/base.py}

当模型服务不支持原生函数调用与结构定义时，系统还可以切换到提示词内嵌 JSON 或 JSON 模式，再用 Pydantic 做最终验证。若结构化输出仍失败，少数适合保底的音乐角色会返回最小可用方案；其他角色抛出包含失败阶段的错误并终止状态传递。

\subsection{可组合工作流}

系统注册了八种预设。它们复用同一组角色，但入口顺序和可选节点随音乐类型变化，如表~\ref{tab:workflows} 所示。

\begin{table}[H]
    \centering
    \small
    \caption{预设工作流的关键路径差异}
    \label{tab:workflows}
    \begin{tabularx}{\textwidth}{L{3.1cm}Y}
        \toprule
        工作流 & 参考南郭和指挥南郭之后的主要节点 \\
        \midrule
        流行人声 & 作词南郭 $\rightarrow$ 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 节奏南郭 $\rightarrow$ 编曲南郭 \\
        古典器乐 & 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 编曲南郭 $\rightarrow$ 可选乐谱导出 \\
        电子器乐 & 节奏南郭 $\rightarrow$ 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 编曲南郭 \\
        影视配乐 & 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 编曲南郭 \\
        爵士合奏 & 和声南郭 $\rightarrow$ 节奏南郭 $\rightarrow$ 旋律南郭 $\rightarrow$ 即兴南郭 $\rightarrow$ 演奏南郭 $\rightarrow$ 编曲南郭 \\
        摇滚人声 & 作词南郭 $\rightarrow$ 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 节奏南郭 $\rightarrow$ 演奏南郭 $\rightarrow$ 编曲南郭 \\
        民谣原声 & 作词南郭 $\rightarrow$ 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 演奏南郭 $\rightarrow$ 编曲南郭 \\
        嘻哈人声 & 节奏南郭 $\rightarrow$ 作词南郭 $\rightarrow$ 旋律南郭 $\rightarrow$ 和声南郭 $\rightarrow$ 演奏南郭 $\rightarrow$ 编曲南郭 \\
        \midrule
        公共收尾 & 音色南郭 $\rightarrow$ 审听南郭 $\rightarrow$ 提示词南郭 \\
        \bottomrule
    \end{tabularx}
\end{table}

图~\ref{fig:pop-flow}--\ref{fig:soundtrack-flow} 对比了三种典型画布。流行人声先由作词南郭完成歌词，再由旋律南郭、和声南郭与节奏南郭依次规划；嘻哈人声则把节奏南郭放在作词南郭之前，先建立节拍与说唱韵律的节奏骨架，并在编曲南郭工作前增加演奏南郭规划以控制说唱咬字、动态和人性化细节；影视配乐跳过作词南郭及常规人声路径，围绕主题旋律、和声张力、配器和空间音色展开。画布显示面向用户的核心创作节点，后端实际执行还包括前置的参考南郭分析。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/流行人声编排.png}
    \caption{流行人声工作流：作词南郭参与协作}
    \label{fig:pop-flow}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/嘻哈人声编排.png}
    \caption{嘻哈人声工作流：节奏南郭先行并加入演奏南郭规划}
    \label{fig:hiphop-flow}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/影视配乐编排.png}
    \caption{影视配乐工作流：围绕情绪叙事与空间声景组织节点}
    \label{fig:soundtrack-flow}
\end{figure}

三张工作流截图给出了直接的对照证据：图~\ref{fig:pop-flow} 中作词南郭位于旋律、和声和编曲之前；图~\ref{fig:hiphop-flow} 将节奏南郭提前，并增加演奏南郭；图~\ref{fig:soundtrack-flow} 则完全省略作词南郭。节点集合与先后顺序都随任务改变。

\subsection{推理与规划方法}

\subsubsection{受约束的计划--执行}

指挥南郭相当于系统的任务规划器。它首先把自然语言需求整理成创意简报，再从固定枚举中选择工作流，并向后续角色下发细分指令。程序将 \verb|workflow| 映射到 LangGraph 中已注册的条件边。模型负责语义判断，程序负责校验和执行，形成受约束的计划--执行过程。

以“创作一首雨夜氛围的电子纯音乐”为例，指挥南郭应得到无人声、电子/氛围、夜色、慢至中速等简报，并选择电子器乐工作流；图执行先由节奏南郭建立节奏骨架，再由旋律南郭与和声南郭继续规划。若需求变为粤语摇滚，则必须选择含作词南郭的人声路径。这个规划过程解决了“同一任务模板无法兼顾所有音乐类型”的问题。

\subsubsection{工具调用式推理与行动}

上传参考音频后，参考南郭使用 \verb|bind_tools| 接收三个白名单工具。模型观察资产名称和索引后决定调用探测、预览或波形工具；程序执行工具并把观察结果写回 \verb|audio_references|，供指挥南郭与编曲南郭使用。这一过程具有“观察--选择动作--取得工具结果”的推理与行动特征，但工具参数只允许 \verb|asset_index|，模型无法提交任意本地路径。

该工具调用方式能够通过 ffprobe 获得稳定可验证的时长、采样率和声道等信息，并把模型的行动空间限制在安全工具集内。系统同时在技能说明中规定，不得从元数据臆测准确和弦、歌词、艺人或配器。

\subsubsection{生成前反思}

审听南郭位于编曲与提示词编译之间。它回看创意简报、旋律、和声、节奏、音色和编曲方案，重点检查人声与器乐冲突、低频拥挤、结构不清、风格漂移以及提示词过度堆砌。提示词南郭再根据这份审听建议取舍内容。该步骤构成一次面向最终输出的反思，使系统在调用付费或耗时的音乐服务前有机会发现全局问题。

\section{记忆机制设计}

\subsection{会话内短期记忆}

每个聊天会话独立保存在 \verb|data/sessions/<session-id>/session.json|，内容包括消息列表、摘要、上传资产和当前活动项目。对话南郭读取最近消息与会话摘要，因此能够回答“这首歌是什么风格”“继续修改上一首”等依赖当前语境的问题。会话之间相互隔离，删除会话时对应资产一并移除。

图~\ref{fig:short-memory} 中，用户在作品完成后追问“这首歌的风格是什么”，系统根据当前会话内已关联的运行结果回答港式摇滚、配器和情绪信息，说明短期上下文同时保存了聊天内容与作品运行关联。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{images/会话内短期记忆.png}
    \caption{会话内短期记忆：根据当前作品回答后续问题}
    \label{fig:short-memory}
\end{figure}

图~\ref{fig:short-memory} 保留了提问发生前的两条生成结果、作品名称、时长和波形，也展示了系统随后对风格、主要乐器与情绪的回答。回答内容与当前作品卡片对应，为“运行结果已关联到会话上下文”提供了可核对的界面证据。

\subsection{跨会话长期记忆}

长期画像存储在 \verb|data/memory/user_profile.json|，主要包含三类信息：

\begin{itemize}
    \item \textbf{语义记忆}：人声/器乐、偏好或避开的乐器、语言、流派、默认时长和制作风格等规范化偏好；
    \item \textbf{行为记忆}：各预设工作流完成次数，用于表现创作习惯；
    \item \textbf{情景记忆}：最近项目和作品的索引。画像保存项目引用，音频文件保留在项目目录。
\end{itemize}

对话南郭只提交用户明确表达的 \verb|MemoryObservation|；\verb|MemoryStore| 再做键规范化、值归一化、去重与冲突替换。同一偏好被重复确认时增加证据次数并提高置信度；同一键出现新的明确值时替换旧值。写入采用临时文件原子替换，避免半写入 JSON。图~\ref{fig:preference-write} 展示“以后使用英语作为创作语言”被识别并提示已记录；图~\ref{fig:memory-view} 则展示长期偏好、置信度、确认次数、工作流计数和最近作品，并提供编辑、单条删除与清空能力。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{images/记忆用户偏好（长期记忆）.png}
    \caption{从明确陈述中写入长期语言偏好}
    \label{fig:preference-write}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/记忆库（长期记忆）.png}
    \caption{长期记忆库：偏好、创作习惯与最近经历}
    \label{fig:memory-view}
\end{figure}

图~\ref{fig:preference-write} 展示了从明确陈述到“偏好已记录”的即时反馈；图~\ref{fig:memory-view} 则显示该语言偏好已经成为可编辑、可删除且带置信度和确认次数的长期条目。后者还列出不同乐团的使用次数与最近创作经历，分别对应语义记忆、行为记忆和情景记忆三种数据。

\subsection{记忆冲突消解}

长期记忆只提供默认值，不能覆盖本次创作的明确要求。系统执行工作流前通过确定性函数解析有效创作偏好，其优先级为

\begin{center}
项目控件 $\succ$ 当前请求 $\succ$ 长期偏好 $\succ$ 系统默认。
\end{center}

其中项目预设可以直接确定人声属性；创作台明确选择的流派、语言和乐器优先；其次从当前文字中识别；只有未指定字段才读取置信度不低于 0.55 的长期偏好。系统还在 \verb|sources| 中记录每个有效值的来源。图~\ref{fig:chat-conflict} 所示实验中，历史画像偏好“纯音乐”，但当前明确要求粤语歌曲，对话南郭选择人声路线，体现了当前意图优先。此规则避免了“记忆越多，系统越不听当前指令”的常见问题。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{images/与南郭先生对话.png}
    \caption{当前粤语人声请求覆盖历史纯音乐偏好}
    \label{fig:chat-conflict}
\end{figure}

图~\ref{fig:chat-conflict} 中，对话回复明确指出当前粤语歌曲要求与既有纯音乐偏好不同，随后实际创建了包含作词南郭的人声工作流并完成音乐生成。界面中的自然语言解释、节点画布和完成状态共同验证了冲突消解规则确实影响了后续执行路径。

\section{工具调用与外部能力}

\subsection{确定性工具分类}

系统将工具按输入输出性质划分为多类，既包含信息检索/探测，也包含计算、媒体变换和生成操作。

\begin{table}[H]
    \centering
    \caption{工具类型及其在工作流中的用途}
    \label{tab:tools}
    \begin{tabularx}{\textwidth}{L{2.8cm}L{5.5cm}Y}
        \toprule
        类型 & 工具 & 作用 \\
        \midrule
        音频探测 & \verb|inspect_audio| & 调用 \verb|ffprobe| 命令返回时长、封装格式、编码、采样率、声道、码率和文件大小。 \\
        音频变换/可视化 & \verb|trim_audio_preview|、\verb|render_waveform| & 生成响度规范化短预览和波形图片，支持参考分析与结果展示。 \\
        确定性音频合成 & \verb|render_prompt_demo_audio| & 从提示词确定三个频率，合成用于中间检查的 12 秒轻量和声音频。 \\
        乐谱计算与导出 & \verb|build_score|、\verb|export_score| & 由乐谱规格构造乐谱，输出 MusicXML 与 MIDI。 \\
        外部音乐生成 & \verb|KieSunoProvider.generate| & 提交结构化请求、轮询任务、下载音频和封面，并处理字段长度限制。 \\
        结果后处理 & \verb|summarize_generated_audio| & 对下载音乐再次探测，并为每条作品产生波形与可展示的技术信息。 \\
        \bottomrule
    \end{tabularx}
\end{table}

图~\ref{fig:audio-input} 中，创作输入支持 MP3、WAV、FLAC、M4A 参考音频；文件进入项目资产目录后，参考南郭只能凭索引调用工具。所有命令都以参数列表启动，不拼接命令行解释器字符串，同时设置超时并检查返回码，从而降低命令注入和挂起风险。

\begin{figure}[H]
    \centering
    \includegraphics[height=0.72\textheight]{images/创作输入——可上传音频（工具调用）.png}
    \caption{创作输入与参考音频上传入口}
    \label{fig:audio-input}
\end{figure}

图~\ref{fig:audio-input} 将作品名称、预设乐团、流派、语言、主要乐器和文字构想集中在同一份结构化输入中，并把参考音频设置为独立上传项。由此可以区分“由用户直接指定的创作约束”和“需要参考南郭调用工具分析的音频资产”，也与前述状态字段分工相对应。

\subsection{乐谱与音乐生成边界}

古典器乐路径在旋律南郭提供乐谱规格且项目存在产物目录时，才进入乐谱导出条件节点。乐谱规格仅允许有限声部、合法速度、拍号、调号、音高、时值和力度，music21 再确定性地导出 \verb|score.musicxml| 和 \verb|score.mid|。因此大模型提出音乐结构，工具负责文件格式细节，缺少乐谱时不会阻断其他工作流。

提示词南郭只负责编译音乐语义，不直接发送网络请求。\verb|KieSunoProvider| 单独处理接口密钥、回调地址、模型字段长度、异步轮询、失败状态、文件安全命名、音频和封面下载。这种供应商隔离使未来替换服务时无需修改作词南郭、旋律南郭或编曲南郭。密钥与服务地址只从环境变量读取，也不会出现在最终提示词中。

\subsection{生成结果的可视化}

工作流得到最终提示词后，应用层先生成一个 12 秒、每分钟 96 拍的本地演示音频，再调用音乐服务。下载完成后，每条曲目通过 ffprobe 读取元数据，并用 FFmpeg 渲染波形。图~\ref{fig:audio-result} 显示同一任务返回两条约 4 分钟音频，它们具有不同的能量分布；用户可播放、比较和下载。演示音频错误记录为 \verb|demo_audio_error|；生成音频后处理错误记录为 \verb|generated_audio_analysis_error|。后处理失败不会抹去已经成功下载的主音乐。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/音乐生成可视化（工具调用）.png}
    \caption{中间演示音频、最终音乐和波形可视化}
    \label{fig:audio-result}
\end{figure}

图~\ref{fig:audio-result} 中，演示音频与两条最终音乐分层呈现，每条最终音乐都带有独立播放器、下载入口、时长、格式、采样率和波形。两条波形的段落能量分布明显不同，证明系统分别保存并分析了两份生成结果。

\section{应用实现与本地数据管理}

\subsection{应用接口运行生命周期}

FastAPI 提供会话、记忆、项目、上传、同步/异步运行和作品集接口。长时间音乐生成采用本地后台线程：请求创建运行记录后立即返回运行标识，前端轮询状态；后端依次把进度更新为工作流、演示音频、音乐生成、音频分析和完成。任一步骤抛出异常时，运行记录会持久化失败状态、阶段和可操作错误；服务重启还会把遗留的运行中状态恢复为中断失败，避免界面永久显示“生成中”。

\noindent\textbf{代码 2：工作流调用与结构化状态构造}（原样引自 \verb|app/api.py|）
\inputminted[firstline=320,lastline=343]{python}{../app/api.py}

为保持本地实验实现简单，系统用一个工作流锁串行保护生成执行，不引入消息队列。这个选择适合单用户课程实验：状态更容易理解，也避免多个耗资源任务并发；代价是多个请求需要排队，后续若扩展多用户服务需改为任务队列或基于进程的执行器。

\subsection{文件系统持久化}

项目数据按以下目录组织：

\begin{minted}[linenos=false]{text}
data/projects/<project-id>/
  project.json
  assets/
  artifacts/score.musicxml, score.mid
  runs/<run-id>.json

data/sessions/<session-id>/session.json, assets/
data/memory/user_profile.json
works/<generated audio, cover, waveform, demo>
\end{minted}

\verb|LocalProjectStore|、\verb|LocalSessionStore| 和 \verb|LocalMemoryStore| 分别成为各自目录结构的唯一管理入口，并在保存 JSON 时先写临时文件再原子替换。运行日志写入 \verb|_logs/|，涵盖应用接口阶段、各南郭的结构化输出、音乐任务提交/轮询和关键错误；数据、音频、密钥与日志均不提交仓库。

\subsection{前端交互闭环}

前端围绕用户任务提供四个相互衔接的页面：

\begin{itemize}
    \item \textbf{对话页}：自然语言讨论、上传参考音频、创建/修改作品并在消息内查看生成进度；
    \item \textbf{创作台}：使用精确表单控制预设、流派、语言、乐器和构想，显示工作流画布与阶段；
    \item \textbf{作品集}：聚合已完成及生成中的作品，提供封面、风格、时长、播放和下载；
    \item \textbf{记忆库}：管理偏好、习惯和最近经历，允许用户纠正或清空记忆。
\end{itemize}

图~\ref{fig:portfolio} 中作品集以最近作品为主视觉，下方排列全部曲目。作品集直接读取本地项目与最新运行记录，使对话中的作品、创作台输出和作品集保持一致。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/作品集.png}
    \caption{本地作品集与作品恢复入口}
    \label{fig:portfolio}
\end{figure}

图~\ref{fig:portfolio} 将最近作品突出展示，同时为其他作品保留封面、完成状态、生成时间、播放与下载入口。这说明生成结果在单次工作流结束后仍可由作品集重新发现和使用，为本地项目持久化与作品恢复提供了界面层证据。

\section{实验过程、结果与分析}

\subsection{实验环境与方法}

后端在 Python 3.12 环境下使用 uv 管理，前端使用 Node.js 与 npm；默认由根目录 \verb|./start.sh| 同时启动 FastAPI（\verb|127.0.0.1:8000|）和 Vite（\verb|127.0.0.1:5173|）。大语言模型使用 OpenAI Chat Completions 兼容接口，结构化输出方式可在函数调用、结构定义、对象模式与提示词内嵌 JSON 间切换；真实音乐生成由配置 KIE API 的 Suno 兼容供应商适配器完成。

实验采用任务场景覆盖法，观察路由、状态交接、工具产物、记忆冲突和作品持久化。下面不再集中罗列用例，而是在介绍每个用例时直接给出对应的实际界面截图和观察结果。

\subsection{工作流差异实验}

\noindent\textbf{用例一：流行/摇滚人声作品。}
输入人声作品需求，观察工作流是否包含作词南郭，生成内容是否保留指定语言和副歌记忆点，并确认最终作品能够播放。实际执行路径如下图所示：画布由指挥南郭进入作词南郭，再逐步进入旋律南郭、和声南郭、节奏南郭与编曲南郭，歌词由此成为旋律与编曲的上游状态。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/流行人声编排.png}
    \caption{实验用例一：流行人声作品的执行路径}
    \label{fig:case-pop-flow}
\end{figure}

\noindent\textbf{用例二：电子纯音乐。}
输入“创作一首雨夜氛围的电子纯音乐”，观察节奏南郭是否先于旋律南郭执行、流程是否跳过作词南郭，以及系统能否生成中间演示音频、最终音乐和波形。下图中系统自动选择电子器乐乐团，工作台状态依次经历建档、参考、生成和完成，最终获得一个 12 秒演示音频与两条 3 分 59 秒音频。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/创作台.png}
    \caption{实验用例二：电子纯音乐的完整执行结果}
    \label{fig:case-electronic-studio}
\end{figure}

\noindent\textbf{用例三：嘻哈人声与影视配乐。}
分别输入嘻哈人声和影视配乐需求，对照观察两条路径的节点差异。嘻哈路径先由节奏南郭建立节拍和能量曲线，再进入作词南郭，并通过演奏南郭约束说唱韵律、动态与人性化；影视配乐路径则跳过作词南郭，避免器乐提示词混入演唱要求。两次执行的画布如下。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/嘻哈人声编排.png}
    \caption{实验用例三：嘻哈人声工作流}
    \label{fig:case-hiphop-flow}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/影视配乐编排.png}
    \caption{实验用例三：影视配乐工作流}
    \label{fig:case-soundtrack-flow}
\end{figure}

这些结果说明条件图能够根据任务改变分工顺序，只运行当前路径需要的南郭。角色复用也控制了实现复杂度：例如同一个和声南郭可在流行、古典、电子和爵士中工作，并根据上游状态与指挥南郭下发的指令生成对应方案。当前前端对古典与影视的核心节点展示较相似，尚未把乐谱导出等后端可选节点直接显示出来，用户对两者细粒度差异的感知仍可增强。

\subsection{记忆实验}

\noindent\textbf{用例四：当前粤语人声要求与长期纯音乐偏好冲突。}
先在长期记忆中记录纯音乐偏好，再明确要求创作粤语人声歌曲，观察当前请求能否覆盖历史偏好。下图中对话回复明确指出“这次要粤语歌，所以会有人声演唱”，随后选择了包含作词南郭的流行人声路径，没有机械继承历史的纯音乐偏好。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{images/与南郭先生对话.png}
    \caption{实验用例四：当前粤语人声请求覆盖长期纯音乐偏好}
    \label{fig:case-memory-conflict}
\end{figure}

\noindent\textbf{用例五：写入并替换长期语言偏好。}
向系统明确提出“以后使用英语作为创作语言”，观察偏好是否写入长期记忆，以及界面能否显示规范化后的值、置信度和证据次数。第一张图展示界面给出“偏好已记录”的即时反馈，第二张图展示英语偏好已经持久化到记忆库。该偏好会影响以后未指定语言的人声作品，但不会覆盖下一次显式选择的中文或粤语。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{images/记忆用户偏好（长期记忆）.png}
    \caption{实验用例五：从明确陈述中写入语言偏好}
    \label{fig:case-preference-write}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/记忆库（长期记忆）.png}
    \caption{实验用例五：长期记忆库中的持久化结果}
    \label{fig:case-memory-view}
\end{figure}

上述结果表明，长期记忆能够保存稳定偏好，同时当前显式请求始终拥有更高优先级，避免历史画像干扰本次创作。

\subsection{工具与产物实验}

\noindent\textbf{用例六：上传参考音频并检查工具产物。}
在创作输入中上传参考音频，观察参考南郭是否只能按资产索引调用探测、预览和波形工具，而不能访问任意路径。第一张图展示参考音频与其他创作参数分开接收；工具返回的是结构化技术事实，并可生成供人工检查的预览和波形。

\begin{figure}[H]
    \centering
    \includegraphics[height=0.72\textheight]{images/创作输入——可上传音频（工具调用）.png}
    \caption{实验用例六：上传参考音频的创作入口}
    \label{fig:case-audio-input}
\end{figure}

最终生成阶段继续调用本地演示音频合成、外部音乐生成、下载音频探测和波形渲染等确定性能力。下图中两条曲目的波形形态明显不同，播放器与波形为比较段落能量提供了直接依据。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/音乐生成可视化（工具调用）.png}
    \caption{实验用例六：演示音频、最终音乐与波形工具产物}
    \label{fig:case-audio-result}
\end{figure}

当前演示音频根据提示词字符确定频率，并混合三个正弦音生成轻量参考。其验证范围包括工具链、格式和播放界面，未覆盖旋律南郭计划的真实试听。参考音频工具目前提供元数据和宏观波形，尚不具备调性、和弦、节拍或乐器识别能力。实验结果证明多智能体与工具链可以完整执行，专业音频理解与编曲质量仍需进一步评价。

\subsection{会话追问与作品恢复实验}

\noindent\textbf{用例七：作品完成后的追问与恢复。}
作品生成完成后，在同一会话中继续追问作品风格、配器与情绪，再进入作品集检查跨会话恢复、播放和下载能力。第一张图显示系统能够结合会话内的工作流运行记录回答作品信息；第二张图显示跨会话后仍可在作品集中发现并使用该作品。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{images/会话内短期记忆.png}
    \caption{实验用例七：根据会话内作品记录回答后续问题}
    \label{fig:case-short-memory}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/作品集.png}
    \caption{实验用例七：在作品集中恢复、播放和下载作品}
    \label{fig:case-portfolio}
\end{figure}

该用例表明，短期记忆负责连续对话，项目文件负责作品事实和跨会话恢复；二者与长期偏好记忆相互补充，使作品信息既能在当前会话中被追问，也能在后续使用中重新发现。

\subsection{综合结果}

\begin{table}[H]
    \centering
    \caption{实验结果的综合评价}
    \label{tab:evaluation}
    \begin{tabularx}{\textwidth}{L{3.0cm}L{3.0cm}Y}
        \toprule
        评价维度 & 实验结果 & 分析 \\
        \midrule
        角色定位 & 完成 & 对话、规划、音乐专业与供应商职责分离，输入输出受模型约束。 \\
        分工与流程 & 完成 & 八种预设复用节点并改变顺序，人声/器乐和爵士/嘻哈等路径有实质差异。 \\
        推理规划 & 完成 & 计划--执行负责全局路由，推理与行动负责工具选择，审听南郭负责生成前反思。 \\
        长短期记忆 & 完成 & 会话上下文与跨会话画像分离，显式优先级规则避免历史覆盖当前。 \\
        工具能力 & 完成 & 覆盖音频探测、音频变换、波形、演示音频、乐谱和外部生成，产物可视化。 \\
        端到端体验 & 完成 & 可从对话或创作台发起任务，查看进度，并在作品集中恢复结果。 \\
        音乐可控性 & 部分完成 & 结构与提示词可控，但最终声学细节仍依赖外部生成模型。 \\
        扩展与并发 & 有限制 & 白名单预设安全清晰，但尚未执行自定义工作流，生成任务当前串行。 \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{结论}

本实验完成了一个可本地运行的多智能体音乐创作系统，并把开放音乐任务转化为有边界的协作：对话南郭负责用户关系与记忆，指挥南郭负责受约束规划，各音乐角色只更新自己的领域状态，审听南郭在生成前做全局审查，提示词南郭与供应商适配器分别承担语义编译和服务调用。LangGraph 条件边使八类音乐工作流能够复用角色并保持差异；短期会话、长期偏好与项目事实共同支持连续创作；多类音频、乐谱和生成工具则把语言规划落实为可播放、可视化、可下载的作品。

从实验结果看，系统已经形成“理解需求--规划分工--专业协作--工具执行--记忆沉淀--作品恢复”的闭环，并能在当前请求与历史偏好冲突时做出合理选择。其不足主要集中在参考音频的乐理理解、反思回路深度、并发扩展和风格安全抽象。上述问题可在现有结构化状态和分层边界上渐进改进。


\clearpage
\end{document}
