# theNanGuos frontend

本地音乐 Agent 创作工作台，使用 React、TypeScript、Vite 和 React Flow。

```bash
npm install
npm run dev
```

开发服务器默认连接 `http://127.0.0.1:8000`。可通过 `VITE_API_BASE` 指定其他 API 地址。

```bash
npm run lint
npm run build
npm run test:e2e
```
